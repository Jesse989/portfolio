import React, { Fragment } from 'react';

const Backend = () => (
  <Fragment>
  <h1 className="white fadeInSlow">I build secure, robust, and well tested backends and micro-services.</h1>

  </Fragment>
)

export default Backend;
